@model List<EnterpriseTypeModel>
@{
    ViewData["Title"] = "Tipo Empresas";
}
<div class="card">
    <div class="card-header">
        Empresas
    </div>
    <div class="card-body">

        <a asp-action="EnterpriseTypeAdd" asp-controller="EnterpriseType" class="btn btn-success btn-sm">Crear un nuevo tipo de empresa Facturadora</a>
        <hr />
        <table class="table">
            <thead>
                <tr>
                    <th>Id</th>
                    <th>Tipo Empresa</th>

                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.etyId</td>
                        <td>@item.etyName</td>
                        <td>
                            <a asp-action="EnterpriseTypeUp" asp-controller="Enterprise" class="btn btn-primary btn-sm" asp-route-Id="@item.etyId">Editar</a>
                        </td>
                        <td>
                        </td>
                    </tr>
                }
                <a asp-action="EnterpriseTypeList" asp-controller="EnterpriseType" class="btn btn-warning btn-sm">Actualizar</a>
                &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;
                <a asp-action="EditorialList" asp-controller="Home" class="btn btn-warning btn-sm">Pagos</a>
                &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;
                <a asp-action="LibroList" asp-controller="Home" class="btn btn-warning btn-sm">Usuarios</a>


            </tbody>
        </table>
    </div>

</div>